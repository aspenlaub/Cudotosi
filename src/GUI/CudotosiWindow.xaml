<Window x:Class="Aspenlaub.Net.GitHub.CSharp.Cudotosi.GUI.CudotosiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CudotosiWindow" Height="450" Width="1000" Icon="cudotosi.ico"
        Loaded="OnLoadedAsync">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="800*" MinWidth="800" />
            <ColumnDefinition Width="100*" MinWidth="100" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Name="Save" Grid.Row="1" Grid.Column="1" Margin="5">_Save</Button>
    </Grid>
</Window>
